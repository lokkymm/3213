import tkinter as tk
import os
import random
import string

def get_current_hwid():
    # Retrieve and return the current HWID
    hwid = "ABCD1234EFGH5678"  # Replace this with actual code to fetch HWID
    return hwid

def spoof_hwid():
    hwid = ''.join(random.choice(string.ascii_uppercase + string.digits) for _ in range(16))
    
    # Code to spoof CPU ID, Disk ID, Network Adapter ID, drive IDs, and MAC address
    # Add the necessary code to spoof these components here
    
    print("HWID, Drives, and MAC Address Spoofed successfully!")

# Create GUI window
root = tk.Tk()
root.title("HWID Spoofer")

# Display current HWID
current_hwid_label = tk.Label(root, text="Current HWID: " + get_current_hwid())
current_hwid_label.pack()

# Button to spoof HWID
spoof_button = tk.Button(root, text="Spoof HWID", command=spoof_hwid)
spoof_button.pack()

root.mainloop()
